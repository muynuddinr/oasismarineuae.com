
version: '3.8'
services:
  nextjs:
    build: .
    ports:
      - "8085:8085"  # Map port 8085 to host port 8085
    environment:
      - NODE_ENV=production
      - PORT=8085
      
      # VPS Configuration with domain and port 8085
      - NEXTAUTH_URL=https://oasismarineuae.com:8085
      - NEXT_PUBLIC_API_BASE_URL=https://oasismarineuae.com:8085/api
      
      # Database Configuration - Set these in your VPS .env file
      - DATABASE_URL=${DATABASE_URL}
      
      # NextAuth Secret - Set in your VPS .env file
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      
      # Google OAuth - Set in your VPS .env file
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      
      # Cloudinary - Set in your VPS .env file
      - CLOUDINARY_CLOUD_NAME=${CLOUDINARY_CLOUD_NAME}
      - CLOUDINARY_API_KEY=${CLOUDINARY_API_KEY}
      - CLOUDINARY_API_SECRET=${CLOUDINARY_API_SECRET}
      
    restart: unless-stopped
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8085/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  app-network:
    driver: bridge
